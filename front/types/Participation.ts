export type ReservationInfo = {
  eventId: number;
  eventTitle: string;
  eventDateTime: string;
  registeredAt: string;
  location: string;
  image: string;
};

export type ParticipationInfo = {
  id: string;
  profileImg: string;
  handleName: string;
};